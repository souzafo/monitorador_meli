name: Monitoramento de Preço

on:
  schedule:
    - cron: "0 3,9,15,21 * * *"  # 00h, 06h, 12h, 18h (horário de Brasília)
  workflow_dispatch:

jobs:
  monitorar-produto:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v2

      - name: Instalar Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"

      - name: Criar ambiente virtual
        run: |
          python3 -m venv ./env  # Criação do ambiente virtual
          ls -l ./env/  # Verifica a estrutura do ambiente virtual
          ls -l ./env/bin/  # Verifica o conteúdo do diretório bin

      - name: Instalar dependências
        run: |
          ./env/bin/pip install --upgrade pip  # Usando pip do ambiente virtual
          ./env/bin/pip install -r requirements.txt  # Instalando as dependências do ambiente virtual

      - name: Definir variáveis de ambiente
        run: |
          echo "TELEGRAM_TOKEN=${{ secrets.TELEGRAM_TOKEN }}" >> $GITHUB_ENV
          echo "TELEGRAM_CHAT_ID=${{ secrets.TELEGRAM_CHAT_ID }}" >> $GITHUB_ENV
          echo "EMAIL_USER=${{ secrets.EMAIL_USER }}" >> $GITHUB_ENV
          echo "EMAIL_PASS=${{ secrets.EMAIL_PASS }}" >> $GITHUB_ENV
          echo "EMAIL_TO=${{ secrets.EMAIL_TO }}" >> $GITHUB_ENV
          echo "PRODUCT_URL=${{ secrets.PRODUCT_URL }}" >> $GITHUB_ENV

      - name: Rodar o script
        run: |
          ./env/bin/python3 main.py  # Rodando o script diretamente com o Python do ambiente virtual
